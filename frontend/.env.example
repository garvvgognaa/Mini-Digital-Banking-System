# Leave empty in development to use Vite proxy (/v1 -> backend).
# For production builds, set the public API origin, e.g. https://api.example.com
VITE_API_URL=

# Dev only: where Vite should proxy /v1 (must match backend PORT in backend/.env).
# VITE_API_PROXY_TARGET=http://127.0.0.1:3000
